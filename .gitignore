# Build output
/build/
/out/
*.user

# JUCE fetched source (re-fetched by CMake)
/_deps/

# OS / IDE
.vs/
.idea/
*.suo
.DS_Store
Thumbs.db

# Locked-DLL rename-swap leftovers
*.old
*.old.tmpv0

# Python analysis tooling artifacts
__pycache__/
*.pyc
analysis.png

# Audio reference/test material — large, kept local only.
# (Use Git LFS if you ever want these tracked.)
*.wav
*.mp3

# NOTE: dist/ holds the shipped VST3 bundle and IS tracked on purpose.
# /build/ above only ignores the build tree, not dist/.
